import {
  __commonJS
} from "./chunk-62VDRKYJ.js";

// node_modules/reduxjs-toolkit-persist/lib/storage/getStorage.js
var require_getStorage = __commonJS({
  "node_modules/reduxjs-toolkit-persist/lib/storage/getStorage.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    function noop() {
    }
    var noopStorage = {
      getItem: noop,
      setItem: noop,
      removeItem: noop,
      keys: [],
      getAllKeys: noop
    };
    function hasStorage(storageType) {
      if (typeof self !== "object" || !(storageType in self)) {
        return false;
      }
      try {
        const storage = self[storageType];
        const testKey = `reduxjs-toolkit-persist ${storageType} test`;
        storage.setItem(testKey, "test");
        storage.getItem(testKey);
        storage.removeItem(testKey);
      } catch (e) {
        if (true)
          console.warn(`reduxjs-toolkit-persist ${storageType} test failed, persistence will be disabled.`);
        return false;
      }
      return true;
    }
    function getStorage(type) {
      const storageType = `${type}Storage`;
      if (hasStorage(storageType))
        return self[storageType];
      else {
        if (true) {
          console.error(`reduxjs-toolkit-persist failed to create sync storage. falling back to noop storage.`);
        }
        return noopStorage;
      }
    }
    exports.default = getStorage;
  }
});

// node_modules/reduxjs-toolkit-persist/lib/storage/createWebStorage.js
var require_createWebStorage = __commonJS({
  "node_modules/reduxjs-toolkit-persist/lib/storage/createWebStorage.js"(exports) {
    "use strict";
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { "default": mod };
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    var getStorage_1 = __importDefault(require_getStorage());
    function createWebStorage(type) {
      const storage = (0, getStorage_1.default)(type);
      return {
        getItem: (key) => {
          return new Promise((resolve) => {
            resolve(storage.getItem(key));
          });
        },
        setItem: (key, item) => {
          return new Promise((resolve) => {
            resolve(storage.setItem(key, item));
          });
        },
        removeItem: (key) => {
          return new Promise((resolve) => {
            resolve(storage.removeItem(key));
          });
        }
      };
    }
    exports.default = createWebStorage;
  }
});

// node_modules/reduxjs-toolkit-persist/lib/storage/session.js
var require_session = __commonJS({
  "node_modules/reduxjs-toolkit-persist/lib/storage/session.js"(exports) {
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { "default": mod };
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    var createWebStorage_1 = __importDefault(require_createWebStorage());
    var webStorage = (0, createWebStorage_1.default)("session");
    exports.default = webStorage;
  }
});
export default require_session();
//# sourceMappingURL=reduxjs-toolkit-persist_lib_storage_session.js.map
